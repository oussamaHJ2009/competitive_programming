#include <bits/stdc++.h>

#define A first
#define B second

using namespace std;

using ll = long long;


void solve() {
    int n,k;
    cin>>n>>k;
    vector<pair<ll,ll>>f(n);
    for(int i=0;i<n;i++){
        cin>>f[i].A>>f[i].B;
    }

    vector<ll>dp(k+1);
    for(int i=0;i<n;i++){
        
    }

}

int main() {
    cin.tie(0)->sync_with_stdio(0);
    solve();
    return 0;
}