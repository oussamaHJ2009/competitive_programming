#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int salta(int N, vector<int> S, vector<int> A, vector<int> B){
	vector<bool>dp(N+1);
	dp[0]=true;
	for (int i = 0; i < N; i++)
	{
		
	}
	
}