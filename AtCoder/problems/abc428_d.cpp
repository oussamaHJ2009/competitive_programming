// lowkey just gave on ts it is lowkey so hard i will come back later T_T

#include <bits/stdc++.h>

using namespace std;
using ll = long long;
const int mod = 1e9 + 7;//998244353

ll f(ll x,ll y){
    return stoll(to_string(x)+to_string(y));
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr); cout.tie(nullptr);

    int T_; cin>>T_;
    while(T_--){
        
    }

    return 0;

}